---
description: Start a new feature in an isolated git worktree on its own feat/<feature-name> branch (sibling dir of the repo root). Use ONLY after the user confirmed (AGENTS.md §12) they want a worktree, not direct-on-main work.
argument-hint: <feature-name>   e.g. search-filters
---

<!-- Thin pointer: the canonical, tool-agnostic copy of this skill lives in .agents/skills/. -->

Read `.agents/skills/worktree-start/SKILL.md` and follow it for **$ARGUMENTS**. See AGENTS.md §12
for the ask-first policy and naming convention.
