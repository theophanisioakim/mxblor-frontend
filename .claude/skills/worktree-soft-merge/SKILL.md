---
description: Squash-merge a completed feature worktree onto main as staged, uncommitted changes (no commit), run tests, then remove the worktree and delete the branch. Use when the user wants the feature diff on main for manual review or their own single commit.
argument-hint: <feature-name>   e.g. search-filters
---

<!-- Thin pointer: the canonical, tool-agnostic copy of this skill lives in .agents/skills/. -->

Read `.agents/skills/worktree-soft-merge/SKILL.md` and follow it for **$ARGUMENTS**. See AGENTS.md §12.
