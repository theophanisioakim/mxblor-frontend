---
description: Merge a completed feature worktree's feat/<feature-name> branch into main from the main worktree, run tests, then remove the worktree and delete the branch. Use when a worktree feature is done and the user wants to keep it.
argument-hint: <feature-name>   e.g. search-filters
---

<!-- Thin pointer: the canonical, tool-agnostic copy of this skill lives in .agents/skills/. -->

Read `.agents/skills/worktree-merge/SKILL.md` and follow it for **$ARGUMENTS**. See AGENTS.md §12.
