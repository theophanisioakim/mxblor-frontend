/**
 * Cross-platform Text — native variant.
 *
 * Re-exports the react-native-reusables `Text` from `@workspace/native-ui`.
 * It reads the `TextClassContext` provided by the native Button so labels
 * inherit the correct foreground color per variant.
 */
export { Text } from "@workspace/native-ui/components/ui/text"
