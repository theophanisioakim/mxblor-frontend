/**
 * Cross-platform View — native variant.
 *
 * Re-exports react-native's `View`. NativeWind augments it with a
 * `className` prop so the same className contract used on web works here.
 */
export { View } from "react-native"
export type { ViewProps } from "react-native"
